# Only needed for `--provider anthropic`. The offline stub provider needs nothing.
# The SDK also accepts ANTHROPIC_AUTH_TOKEN or a profile stored by `ant auth login`.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Sets the default for --judge-model; the flag still wins when passed.
# PROMPTPROOF_JUDGE_MODEL=claude-opus-5
